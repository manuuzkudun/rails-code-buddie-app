<%= form_for @work_space do |f| %>

  <%= f.label :name %>:
  <%= f.text_field :name %><br />

  <%= f.label :language %>:
  <%= f.collection_select :language, Language.all, :id, :name, prompt: true %>

  <%= f.submit %>
<% end %>
